{
	"scope": "text.html.embedded.coi meta.tag - comment - string",
	"completions": [
		{ "trigger": "if", "kind": ["markup", "k", "Keyword"], "details": "flow control tags" },
		{ "trigger": "else", "kind": ["markup", "k", "Keyword"], "details": "flow control tags" },
		{ "trigger": "for", "kind": ["markup", "k", "Keyword"], "details": "flow control tags" },
	]
}
